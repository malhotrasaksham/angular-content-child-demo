import { Component } from "@angular/core";

@Component({
  selector: "my-app",
  templateUrl: "./app.component.html",
  styleUrls: ["./app.component.css"]
})
export class AppComponent {
  //demo ng container
  //multiple siglings can go into default selector
  //multiple slots with same selectors is not repeated
}
